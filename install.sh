Users can curl this & run it to install flamelex

- pull git repo
- install dependencies (including external deps e.g. repgrip)
- build flamelex
- install flamelex (flx command in command line)

I Guess these ones should be in app...

- (optional) create a local memex
- (optional) sign up for flamelex Cloud
- (optional) opt-in to answer surveys periodically - this helps us build better product and improve user experience, and helps us understand how you use flamelex / useful data we might sell / companies might pay us to collect - but it helps the project out
- (optional) opt-in to telemetry
